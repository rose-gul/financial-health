# Copy this file to .env and fill in a key ONLY if you want to enable the optional LLM path.
# The pipeline runs fully offline (rule-based) without any key.
#
# The key is injected ONLY into the categorize and advice agents' subprocess environments.
# ingest / forecast / alerts / report never receive it and cannot reach Claude.
ANTHROPIC_API_KEY=
